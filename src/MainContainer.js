import React, { Component } from 'react';

class MainContainer extends Component {
  render() {
    return (
      <div >
        This is MainContainer component
      </div>
    );
  }
}

export default MainContainer;
